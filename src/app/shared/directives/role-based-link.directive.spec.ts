import { RoleBasedLinkDirective } from './role-based-link.directive';

describe('RoleBasedLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new RoleBasedLinkDirective();
    expect(directive).toBeTruthy();
  });
});
